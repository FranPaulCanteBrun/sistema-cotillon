# ============================================
# Cotillón Manager - Backend Configuration
# ============================================
# Copia este archivo como .env y ajusta los valores

# Base de datos PostgreSQL
# Formato: postgresql://USUARIO:PASSWORD@HOST:PUERTO/NOMBRE_DB
DATABASE_URL="postgresql://cotillon:cotillon123@localhost:5432/cotillon_db?schema=public"

# JWT Secret - IMPORTANTE: Cambiar en producción
# Debe tener al menos 32 caracteres
JWT_SECRET="desarrollo-local-cambiar-en-produccion-32chars"

# Configuración del servidor
PORT=3000
HOST="0.0.0.0"

# Entorno: development | production | test
NODE_ENV="development"

# ============================================
# Configuración AFIP/ARCA (Facturación Electrónica)
# ============================================
# Estas variables son OPCIONALES. Solo configúralas si vas a usar facturación electrónica.
# Si alguna está presente, TODAS deben estar configuradas.

# Entorno AFIP: 'homo' = homologación (desarrollo/pruebas), 'prod' = producción
# En homologación se usa el CUIT del desarrollador para pruebas
AFIP_ENV="homo"

# CUIT del emisor (formato: 20-12345678-9 o 20123456789)
# En homologación: usar el CUIT/CUIL del desarrollador (monotributista)
# En producción: usar el CUIT del cliente final
# AFIP_CUIT="20-12345678-9"

# Certificado P12 en Base64
# Para obtenerlo:
# 1. Descargar el certificado .p12 desde AFIP
# 2. Convertir a Base64: cat certificado.p12 | base64 -w 0 (Linux/Mac) o certutil -encode certificado.p12 certificado.txt (Windows)
# 3. Copiar el contenido (sin saltos de línea) aquí
# AFIP_CERT_P12_BASE64="MIIKpAIBAzCCCl4GCSqGSIb3DQEHAaCCCk8EggpLMIIKRzCCBXcGCSqGSIb3..."

# Contraseña del certificado P12
# La misma que usaste al descargar el certificado desde AFIP
# AFIP_CERT_P12_PASSWORD="tu_contraseña_secreta"

# Punto de venta (OPCIONAL)
# Si no se especifica, se descubrirá automáticamente usando FEParamGetPtosVenta
# En homologación generalmente es 1, pero puede variar
# AFIP_PTO_VTA=1

